{% extends 'layout.html.twig' %}

{% block content %}
<div class="container">
    <div class="panel">
        <div class="row">
            <div class="col-md-12">
                <h1 class="panel-title">Editar Categoria</h1>
                {% set action = route('category-costs.update', {'id': category.id})  %}
                {% include 'category-costs/_form.html.twig' %}
            </div>
        </div>
    </div>
</div>
{% endblock %}